# Filled at generation time per the copier `providers` selection
# (agentic-platform-plan.md §1.6). Each tier is a fallback chain of
# "provider:model" strings — agentcore.llm.ModelRouter tries them in
# order via langchain.chat_models.init_chat_model.
tiers:
  light:
    - "anthropic:claude-haiku-4-5"
  versatile:
    - "anthropic:claude-sonnet-5"
  reasoning:
    - "anthropic:claude-opus-5"
  auto:
    - "anthropic:claude-sonnet-5"
